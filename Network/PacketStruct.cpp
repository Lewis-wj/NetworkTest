//
// Created by lewis_cjv4sqf on 02/12/2023.
//

#include "PacketStruct.h"

/*sf::Packet& operator <<(std::basic_string<char> packet, const Message& msg)
{
  return packet << MESSAGE << msg.username << msg.content;
}

sf::Packet& operator >>(sf::Packet& packet, Message& msg)
{
  return packet >> msg.username >> msg.content;
}*/

//sf::Packet& operator <<(sf::Packet& packet, const Movement& mvm)
//{
//  return packet << MOVEMENT << mvm.username << mvm.position;
//}
//
//sf::Packet& operator >>(sf::Packet& packet, Movement& mvm)
//{
//  return packet >> mvm.username >> mvm.position;
//}